<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<% @tasks.each do |task| %>
    <p><%= link_to task.name, task_path(task.id) %></p>
    <p><%= task.detail %></p>
    <p><%= task.deadline_at %></p>
<% end %>

<%= link_to "タスク作成", new_task_path %>